# Скопируйте в .env и заполните реальными значениями. .env НЕ должен попадать в git.

POSTGRES_DB=vtoroe_dyhanie
POSTGRES_USER=vtoroe_app
POSTGRES_PASSWORD=change_me
POSTGRES_HOST=db
POSTGRES_PORT=5432

# openssl rand -hex 32
SECRET_KEY=change_me

# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DATA_ENCRYPTION_KEY=change_me

ADMIN_LOGIN=admin
# python -c "import bcrypt; print(bcrypt.hashpw(b'ваш_пароль', bcrypt.gensalt()).decode())"
ADMIN_PASSWORD_HASH=change_me

ALLOWED_ORIGINS=https://vtoroedyhaniecenter.ru,https://www.vtoroedyhaniecenter.ru
COOKIE_SECURE=true
