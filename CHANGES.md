# Что изменено

- Цитата перенесена перед главным экраном.
- Убрана фраза «Шанс. Поддержка. Новая жизнь.» с главного экрана.
- Убран визуальный логотип/бренд из шапки главной страницы.
- Убран блок «Наш подход».
- Добавлены три редактируемых текстовых блока на главной.
- Пустые строки в описаниях выводятся как отдельные абзацы.
- Убран текст о согласии на обработку персональных данных под формой.
- В футере оставлено только `© 2026 «Второе дыхание»`.
- Экран входа теперь называется «Служебный вход».
- В панели убрана надпись «Управление сайтом».
- Добавлен favicon из предоставленного логотипа.
- В админ-панели добавлено управление кадрированием главного изображения: горизонталь, вертикаль и масштаб.
- Настройки изображения сохраняются в PostgreSQL и применяются на ПК и телефоне.
- Обновлена SEO-метаинформация.
- Обновлены `robots.txt` и `sitemap.xml`.
- Добавлена структурированная информация Schema.org.
- В разделе Instagram добавлена требуемая юридическая пометка.

- Исправлено масштабирование главного изображения: вместо процентного значения CSS scale теперь используется числовой коэффициент, поэтому фото больше не уменьшается до миниатюры.

- На мобильных экранах главное фото дополнительно масштабируется на 18%, чтобы кадр не выглядел слишком широким; сохранены выбранные в админке точка фокуса и масштаб.

### Hero image framing — rebuilt (fixes repeated mis-cropping/stretching)
Предыдущие фиксы переключали `.hero-image` между `object-fit:cover` и `object-fit:fill` — оба варианта ломали кадрирование, потому что блок `.hero` не имеет фиксированного соотношения сторон (`height:clamp(...)` + `width:100%`), а бэкенд обрезал фото под жёсткие 16:9/9:16. В результате уже обрезанное фото браузер обрезал/растягивал ещё раз, и итог расходился с тем, что было видно в редакторе кадрирования в админке.

Новая схема:
- Бэкенд больше не обрезает фото под фиксированное соотношение сторон. При загрузке он только выравнивает EXIF-поворот и уменьшает слишком большие фото (не длиннее 2600px по большей стороне), сохраняя полный кадр.
- Кадрирование и масштаб (`hero_position_x/y`, `hero_zoom`, а также отдельные `hero_mobile_*` для телефона) применяются живым CSS (`object-position` + `transform: scale`) прямо на странице через CSS-переменные, а не через отдельные обрезанные файлы для десктопа и телефона.
- Тот же CSS-механизм уже использовался в превью редактора кадрирования в админке — теперь превью и то, что видят посетители сайта, всегда совпадают, на любом экране и при любых пропорциях блока `.hero`.
- `object-fit: cover` оставлен только как подстраховка от пустых полос, а не как основной механизм кадрирования.
